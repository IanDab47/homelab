# Lessons Learned

- VLAN separation simplifies firewall rules
- WireGuard is lightweight but keys must never be committed
- Testing containerized apps locally avoids accidental WAN exposure
- Document everything in Markdown for reproducibility